# Структура проекта и инженерные правила

Документ описывает текущую структуру репозитория и правила совместной работы.
Как устроен расчёт, описано в [design.md](design.md), откуда берутся данные —
в [data-profile.md](data-profile.md).

Владельцы: **А** — Aliar (ядро, данные, AI, Docker), **Б** — API, **Ф** — фронт и README.

## Дерево

```
hack-aa7e8423-grimstack/
├── README.md                  для жюри: запуск, проверка за 5 минут, архитектура,
│                              таблица кодов ошибок, ограничения, внешние материалы  [Ф]
├── requirements.txt           зависимости с версиями, без torch                      [Б]
├── .env.example               OPENAI_API_KEY, OPENAI_BASE_URL, MODEL, PORT           [Б]
├── Dockerfile, .dockerignore  один образ, один процесс, запуск python -m app         [А]
│
├── app/                       моносервис: один процесс FastAPI
│   ├── __main__.py            python -m app → uvicorn на 0.0.0.0:$PORT               [Б]
│   ├── main.py                сборка приложения: /health, единый обработчик ошибок,
│   │                          роутеры /api, раздача frontend/dist (если собран)      [Б]
│   ├── store.py               Dataset в памяти (грузится один раз при старте),
│   │                          прогоны в памяти, утверждения в SQLite var/app.db      [Б]
│   ├── contracts.py           Pydantic-контракт ядро ↔ API ↔ фронт, заморожен        [А]
│   │
│   ├── api/                   HTTP-слой: тонкие роутеры, логики расчёта нет          [Б]
│   │   ├── routes.py          расчёт, сравнение сценариев, правка строки, утверждение,
│   │   │                      экспорт XLSX, история SKU, сводка, загрузка, бэктест
│   │   └── schemas.py         модели запросов и ответов, которых нет в контракте
│   │
│   ├── engine/                домен: чистая логика расчёта, про HTTP не знает         [А]
│   │   ├── cleaning.py        паллетный поток SE, разовые крупные строки
│   │   ├── stockout.py        месяцы без товара, восстановление упущенного спроса
│   │   ├── forecast.py        база, сезонность, тренд, сегменты спроса
│   │   ├── policy.py          точка заказа, страховой запас, кратность, срочность
│   │   ├── explain.py         «водопад» и текст обоснования строки
│   │   ├── pipeline.py        сборка шагов в RunResult, история SKU, Meta
│   │   └── config.py          сроки поставки, уровни сервиса по категориям
│   │
│   ├── ingest/                инфраструктура: выгрузки 1С → Dataset                   [А]
│   │   ├── iek.py, se.py      загрузчик на поставщика: новый поставщик = новый файл
│   │   ├── common.py          месяцы из заголовков 1С, код, хэш документа, накладные
│   │   └── dataset.py         канонический Dataset и concat()
│   │
│   └── ai/                    AI-слой, про HTTP не знает                              [А]
│       ├── llm.py             LLM-сводка по заказу поставщику с кэшем ответов
│       └── groups.py          товарные группы: правила по ключевым словам + выбор между
│                              правилом и ответом Laya
│
├── frontend/                  Vite + React + TypeScript                              [Ф]
│   ├── src/api/               types.ts (зеркало contracts.py), live- и mock-клиент
│   ├── src/features/          детали SKU и графики; остальные сценарии в App.tsx
│   ├── src/shared/            форматирование, кратность, общие UI-компоненты
│   └── dist/                  собранная SPA, коммитится: её раздаёт FastAPI
│
├── data/
│   ├── raw/{iek,se}/<role>.xlsx   выгрузки партнёра                                  [А]
│   ├── categories/            sku_categories.csv — товарные группы (правила + Laya),
│   │                          laya_eval.json — замер качества Laya                   [А]
│   └── backtest/report.json   результаты бэктеста для README и слайдов               [А]
├── samples/cache/             проверенные образцы сводок для запуска без ключа       [А]
├── scripts/
│   ├── classify_laya.py       офлайн-классификация наименований, torch только здесь   [А]
│   ├── warm_llm_cache.py      прогрев samples/cache/ живыми ответами LLM перед сдачей  [А]
│   └── backtest.py            «машина времени»: наш метод против Excel на истории    [А]
├── contracts/*.json           примеры ответов API из реального прогона               [А]
├── docs/
│   ├── design.md              архитектура, алгоритм, контракт, ограничения           [А]
│   ├── API.md                 фактические HTTP-маршруты и коды ошибок                [Б]
│   ├── data-profile.md        устройство выгрузок и правила их разбора               [А]
│   ├── structure.md           этот документ                                          [А]
│   ├── backtest.md            метод и результаты бэктеста                            [А]
│   ├── plans/, tasks/         план ядра, задания участникам                          [А]
└── tests/
    ├── test_contracts.py      моки contracts/*.json соответствуют моделям            [А]
    ├── engine/                юнит-тесты, приёмка пяти must-have, реальные данные,
    │                          бэктест без утечки будущего                           [А]
    ├── api/                   по тесту на каждый код ответа                          [Б]
    └── test_boundaries.py     домен и инфраструктура не импортируют FastAPI           [А]
```

В репозиторий не попадают `var/` (утверждения, файлы времени работы), `.env`, `node_modules/`,
`.venv/` и кэши тестов.

## Правила

**Слои и зависимости.** Зависимости идут только вниз: `api → engine → ingest`.
- `engine` и `ingest` не импортируют FastAPI; это проверяет `tests/test_boundaries.py`.
- Роутер получает параметры, вызывает одну функцию ядра и возвращает модель из
  `contracts.py`. Считать в роутере нельзя.
- Ядро вызывается только через `engine.run / history / meta` и
  `ingest.load_default / load_uploaded`.

**Контракт один.** Формы данных между частями задаёт только `app/contracts.py`.
- Фронт повторяет его в `src/api/types.ts`.
- Изменение контракта согласуют все трое. После него перегенерируются `contracts/*.json`,
  а `tests/test_contracts.py` должен оставаться зелёным.

**Ошибки в одном формате.** Любая ошибка API отвечает `{"detail", "code", "meta"}`.
- Коды загрузки задаёт ядро через `IngestError`: `unknown_role`, `missing_file`,
  `empty_file`, `not_xlsx`, `bad_format`. Ответ — 422, роль файла передаётся в `meta.file_role`.
- Остальные коды задаёт API: 404, 409, 422, 503, 500.
- Полная таблица кодов живёт в одном месте — в README.
- При 500 наружу не уходит трассировка, она пишется в лог.

**Данные грузятся один раз.** Разбор всех xlsx занимает около 11 секунд. Поэтому `Dataset`
создаётся при старте процесса и хранится в памяти, а не читается заново на каждый запрос.
Пути к данным строятся от расположения кода, а не от текущей папки.

**Честность результата.** Сервис не выдаёт оценку за факт.
- Остаток IEK помечается как нижняя граница.
- Отсутствующая цена показывается как «нет данных», а не как ноль.
- Ответ LLM из кэша помечается.
- Без ключа и без кэша сводка честно недоступна (503), а расчёт работает полностью.

**Тесты перед каждым коммитом.** Одна команда из корня: `pytest -q`. Каждая проверка из ТЗ —
отдельный тест в `tests/engine/test_acceptance.py`. Инварианты ответа:
- сумма qty-компонентов равна `recommended_qty`;
- количество не меньше нуля и кратно `moq`;
- нет NaN.

**Воспроизводимость.** Жюри выполняет ровно две команды:
`pip install -r requirements.txt` и `python -m app`.
- Node не нужен, потому что `frontend/dist` закоммичен.
- Ключи не нужны: LLM опциональна.
- Тяжёлые зависимости (torch, transformers) в `requirements.txt` не добавляются.
- Второй способ запуска — Docker-образ из того же репозитория.

**Git.** Работаем в `main` без веток.
- Каждый правит только свои файлы и перед коммитом делает `git pull --rebase`.
- Коммит — каждый час, каждый под своим аккаунтом (п. 6.5–6.6 Положения).

## Чего не делаем

Не делаем:
- отдельный сервер БД: утверждения лежат в SQLite `var/app.db`, прогоны живут в памяти;
- авторизацию, историю прогонов, переключатель светлой и тёмной темы, мультиязычность;
- реестры и абстракции «на будущее»;
- автоматическую отправку заказа поставщику: её запрещает ТЗ.
